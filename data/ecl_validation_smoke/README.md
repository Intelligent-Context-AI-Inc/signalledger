# SignalLedger validation smoke input

This directory intentionally exists to trigger the public PR workflow path during validation. The root `ecl-trainer.manifest.json` remains the manifest location consumed by the Action.
