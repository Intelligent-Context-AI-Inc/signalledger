# ECL Learning Ledger Interactive Demo

This folder is a seed for a tiny public demo repository.

## Demo PR 1: Safe Metadata

Commit `safe-manifest.json`. The expected result is a local artifact bundle and a PR comment in `report_only` mode.

## Demo PR 2: Payload Trap

Commit `bad-manifest.payload-demo.json`. The expected result is a No-Payload failure before ledger append.

## Boundaries

The unsafe file uses a controlled placeholder marker. It is not a dataset sample, prompt, completion, embedding, token sequence, or model weight.
