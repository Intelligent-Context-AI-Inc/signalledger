# Compliance Passport Example

Generate a metadata-only passport from the local ledger:

```bash
ecl-trainer passport --project-namespace org/project --ledger-path .ecl/events.jsonl --profile model_release_signoff
```
